<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all.sort_by{ |ing| ing }, prompt: "Choose an ingredient" %>
  <%= f.button :submit %>
<% end %>
