<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all.sort, prompt: "Choose an ingredient" %>
  <%= f.button :submit %>
<% end %>
